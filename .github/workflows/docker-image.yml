name: Docker Image CI_CD

on:
  push:
    branches: [ "CI_CD" ]

jobs:

  test:
    name: Jest test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Install modules
      run: yarn
    - name: Run tests
      run: yarn test
  build_docker_image:
    name: build docker image and push to Docker Hub
    needs: test
    runs-on: ubuntu-latest
    steps:
    - name: Set up Docker Buildx
    - uses: docker/setup-buildx-action@v2
    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    - name: Build and push base
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: thelittlesheep/onlinemenu:base
        target: base
    - name: Build and push dev
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: thelittlesheep/onlinemenu:dev
        target: dev
    # - name: Build the base Docker image
    #   run: docker build . --file Dockerfile --target base --tag thelittlesheep/onlinemenu:base
    # - name: Build the dev Docker image
    #   run: docker build . --file Dockerfile --target dev --tag thelittlesheep/onlinemenu:dev